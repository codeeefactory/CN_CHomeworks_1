#pragma once
#include <fstream>
#include <iostream>
#include <regex>
#include <vector>
using namespace std;
class ClientCommands {
private:

public:
	int commandreader(string command) {
		vector<string> cmds;
		extern std::string argument1;
		extern std::string argument2;
	}
};